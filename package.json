{
  "name": "dungeons-and-components",
  "version": "0.5.0",
  "description": "A React component library for Dungeons and Dragons.",
  "author": "Christopher Baker <christpher@hmudesign.com>",
  "license": "MIT",
  "homepage": "https://github.com/HMUDesign/dungeons-and-components#readme",
  "bugs": {
    "url": "https://github.com/HMUDesign/dungeons-and-components/issues"
  },
  "scripts": {
    "eslint": "eslint .",
    "depcheck": "depcheck --ignore-dirs=lib --ignores=~,@storybook/* .",
    "storybook": "start-storybook -p 6006",
    "clean": "rm -rf lib",
    "build": "npm run clean && NODE_ENV=production babel --source-maps --copy-files src --out-dir lib",
    "dev": "npm run clean && NODE_ENV=development babel --source-maps --copy-files src --out-dir lib  --watch",
    "docs": "npm run build && build-storybook -o public && gh-pages -m 'Updates [ci skip]' -d public",
    "release:major": "npm version major && git push && git push --tags && npm publish",
    "release:minor": "npm version minor && git push && git push --tags && npm publish",
    "release:patch": "npm version patch && git push && git push --tags && npm publish",
    "release:pre": "npm version prerelease && git push && git push --tags && npm publish --tag=pre",
    "prepare": "npm run build"
  },
  "eslintConfig": {
    "root": true,
    "extends": "@hmudesign/eslint-config"
  },
  "main": "lib/dnc.js",
  "peerDependencies": {
    "prop-types": "^15.7.2",
    "react": "^16.12.0"
  },
  "dependencies": {
    "@emotion/core": "^10.0.22",
    "@emotion/styled": "^10.0.23",
    "lodash": "^4.17.15"
  },
  "devDependencies": {
    "@babel/cli": "^7.7.4",
    "@babel/core": "^7.7.4",
    "@babel/plugin-proposal-optional-chaining": "^7.7.4",
    "@hmudesign/eslint-config": "^1.0.0",
    "@storybook/addon-docs": "^5.2.6",
    "@storybook/addons": "^5.2.6",
    "@storybook/react": "^5.2.6",
    "@storybook/theming": "^5.2.8",
    "babel-loader": "^8.0.6",
    "babel-plugin-emotion": "^10.0.23",
    "babel-preset-react-app": "^9.0.2",
    "depcheck": "^1.0.0",
    "eslint": "^7.5.0",
    "gh-pages": "^3.1.0",
    "prop-types": "^15.7.2",
    "react": "^16.12.0",
    "react-dom": "^16.12.0"
  }
}
